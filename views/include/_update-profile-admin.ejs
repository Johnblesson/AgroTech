<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="./favicon.svg" type="image/svg+xml">
  <title>Update profile</title>
  <link rel="stylesheet" href="./css/form.css">
  <style>
    /* Card Styling */
    .form-card {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      width: 80%;
      max-width: 700px; /* Increased width */
      margin: 20px auto;
    }

    .formbold-form-wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: #f7f7f7;
    }
  </style>
</head>
<body>
<div class="formbold-form-wrapper">
  <!-- Card -->
  <div class="form-card">

    <!-- Heading -->
    <h1 style="text-align: center; font-size: 20px; margin-bottom: 20px;">PROFILE DETAILS</h1>

    <!-- User's Photo -->
    <img 
      src="<%= users.photo %>" 
      alt="User's Photo" 
      style="display: block; margin: 0 auto 10px; border-radius: 50%; width: 100px; height: 100px; object-fit: cover;" 
    />

    <!-- Paragraph -->
    <p style="text-align: center; font-size: 14px; color: #555; margin-bottom: 20px;">Update your details below</p>

    <!-- Form -->
    <form action="/update-profile/<%= users._id %>?_method=PATCH" method="POST" class="edit-profile-form">
      <input type="hidden" name="aid" id="aid" class="formbold-form-input" />

      <div class="formbold-mb-3">
        <label for="title" class="formbold-form-label"> Fullname: </label>
        <input type="text" name="title" id="title" value="<%= users.fullname %>" class="formbold-form-input" readonly/>
      </div>

      <div class="formbold-mb-3">
        <label for="username" class="formbold-form-label"> Username: </label>
        <input type="text" name="username" id="username" value="<%= users.username %>" class="formbold-form-input" readonly/>
      </div>

      
        <div class="formbold-mb-3">
          <label for="district" class="formbold-form-label">Place of Birth:</label>
          <div>
          <select id="district" class="formbold-form-input" name="placeOfBirth">
            <option value="" disabled selected>Select a hometown</option>
            <option value="Bo">Bo, Sierra Leone</option>
            <option value="Bombali">Bombali, Sierra Leone</option>
            <option value="Bonthe">Bonthe, Sierra Leone</option>
            <option value="Falaba">Falaba, Sierra Leone</option>
            <option value="Kailahun">Kailahun, Sierra Leone</option>
            <option value="Kambia">Kambia, Sierra Leone</option>
            <option value="Karene">Karene, Sierra Leone</option>
            <option value="Kenema">Kenema, Sierra Leone</option>
            <option value="Koinadugu">Koinadugu, Sierra Leone</option>
            <option value="Kono">Kono, Sierra Leone</option>
            <option value="Moyamba">Moyamba, Sierra Leone</option>
            <option value="Port Loko">Port Loko, Sierra Leone</option>
            <option value="Pujehun">Pujehun, Sierra Leone</option>
            <option value="Tonkolili">Tonkolili, Sierra Leone</option>
            <option value="Western Area Rural">Western Area Rural, Sierra Leone</option>
            <option value="Western Area Urban">Western Area Urban, Sierra Leone</option>
            <option value="Other">Other</option>
          </select>
        </div>
      </div>

      <div class="formbold-mb-3">
        <label for="country" class="formbold-form-label"> Country: </label>
        <input type="text" name="country" id="country" value="<%= users.country %>" class="formbold-form-input" readonly/>
      </div>
      
      <div class="formbold-mb-3">
        <label for="email" class="formbold-form-label"> Email: </label>
        <input type="email" name="email" id="location" value="<%= users.email %>" class="formbold-form-input" />
      </div>

      <div class="formbold-mb-3">
        <label for="phone" class="formbold-form-label"> Phone: </label>
        <input type="text" name="phone" id="phone" value="<%= users.phone %>" class="formbold-form-input" readonly/>
      </div>

      <div class="formbold-mb-3">
        <label for="status" class="formbold-form-label"> Status: </label>
        <div>
          <select id="status" class="formbold-form-input" name="status">
            <option value="" disabled selected hidden>Select Status</option>
            <option value="active">Active</option>
            <option value="inactive">Inactive</option>
          </select>
        </div>
      </div>

      <div class="formbold-mb-3">
        <label for="bio" class="formbold-form-label"> Bio: </label>
        <textarea rows="6" name="bio" id="bio" placeholder="Add bio..." class="formbold-form-input"><%= users.bio %></textarea>
      </div>

      <button type="submit" class="formbold-btn">SAVE DETAILS</button>
    </form>
  </div>
</div>
<script>
  function handleDistrictChange() {
      const districtSelect = document.getElementById("district");
      const otherDistrictGroup = document.getElementById("otherDistrictGroup");
      const selectedValue = districtSelect.value;

      if (selectedValue === "Other") {
          otherDistrictGroup.style.display = "block";
      } else {
          otherDistrictGroup.style.display = "none";
      }
  }
</script>
</body>
</html>
