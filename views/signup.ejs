<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AgroTech SL | Create Account</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="shortcut icon" href="/favicon.svg" type="image/svg+xml">
  <meta name="description" content="Join AgroTech SL to connect with farmers, buyers, and agricultural opportunities across Sierra Leone. Empower your farming journey with innovation." />
  <meta name="keywords" content="AgroTech, Sierra Leone, farmers, agricultural innovation, farm marketplace, crop trading, digital farming, technology in agriculture" />
  <meta name="author" content="John Blesson Rowe" />

  <style>
    body {
      background: linear-gradient(135deg, #e6f2e6, #d1ffd6);
      font-family: 'Poppins', sans-serif;
    }

    .auth-container {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    .card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 480px;
      background: #fff;
    }

    .card-header {
      background: transparent;
      text-align: center;
      border-bottom: none;
      padding-top: 30px;
    }

    .card-header .logo {
      font-size: 1.8rem;
      font-weight: bold;
      color: #2e8b57;
      text-decoration: none;
    }

    .card-header p {
      color: #555;
      font-size: 0.9rem;
    }

    .card-body {
      padding: 2rem;
    }

    .form-label span {
      color: red;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px 15px;
    }

    .btn-success {
      background-color: #2e8b57;
      border: none;
      border-radius: 10px;
      font-weight: 600;
      transition: 0.3s;
    }

    .btn-success:hover {
      background-color: #267347;
    }

    .terms {
      display: block;
      font-size: 0.8rem;
      text-align: center;
      color: #777;
      margin-top: 10px;
    }

    .redirect-text {
      text-align: center;
      margin-top: 15px;
    }

    .error-message {
      background: #ffe5e5;
      border-left: 4px solid #ff4d4d;
      padding: 10px;
      border-radius: 5px;
      color: #a94442;
      margin-bottom: 1rem;
    }

    .file-input-label {
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #f5f5f5;
      border: 1px dashed #bbb;
      padding: 10px;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .file-input-label:hover {
      background-color: #e9f7ef;
      border-color: #2e8b57;
    }

    .toggle-password {
      position: absolute;
      right: 15px;
      top: 40px;
      cursor: pointer;
      color: #aaa;
    }

  </style>
</head>

<body>
  <div class="auth-container">
    <div class="card">
      <div class="card-header">
        <a href="/" class="logo"><i class="fa-solid fa-seedling"></i> AgroTech SL</a>
        <h4 class="mt-2 fw-bold">Create Your Account</h4>
        <p>Join our agricultural marketplace and connect with opportunities.</p>
      </div>

      <div class="card-body">

        <% if (errorMessages && errorMessages.length > 0) { %>
          <div class="error-message">
            <% errorMessages.forEach(function(message) { %>
              <p><%= message %></p>
            <% }); %>
          </div>
        <% } %>

        <form action="/signup" method="POST" enctype="multipart/form-data">
          <div class="mb-3">
            <label class="form-label">Full Name <span>*</span></label>
            <input type="text" name="fullname" class="form-control" placeholder="Enter your full name" required />
          </div>

          <div class="mb-3">
            <label class="form-label">Username <span>*</span></label>
            <input type="text" name="username" class="form-control" placeholder="Choose a username" required />
          </div>

          <div class="mb-3">
            <label class="form-label">Email (optional)</label>
            <input type="email" name="email" class="form-control" placeholder="Enter your email" />
          </div>

          <div class="mb-3">
            <label class="form-label">Phone Number <span>*</span></label>
            <input type="text" name="phone" class="form-control" placeholder="Enter your phone number" required />
          </div>

          <div class="mb-3 position-relative">
            <label class="form-label">Password <span>*</span></label>
            <input type="password" id="password" name="password" class="form-control" placeholder="Enter your password" required />
            <i class="toggle-password fas fa-eye-slash" onclick="togglePassword()"></i>

            <div id="password-requirements" class="mt-2 small">
              <strong>Password Requirements:</strong>
              <ul class="list-unstyled">
                <li id="length" class="text-danger">Minimum 6 characters</li>
                <li id="uppercase" class="text-danger">At least one uppercase letter</li>
                <li id="lowercase" class="text-danger">At least one lowercase letter</li>
                <li id="number" class="text-danger">At least one number</li>
              </ul>
            </div>
          </div>

          <div class="mb-3">
            <label class="form-label">Profile Photo <span>*</span></label>
            <input type="file" name="photo" accept="image/*" class="form-control" required />
          </div>

          <div class="d-grid">
            <button type="submit" class="btn btn-success btn-lg">Create Account</button>
          </div>

          <p class="redirect-text mt-3">
            Already have an account? <a href="/login" class="text-success fw-semibold">Login here</a>
          </p>

          <small class="terms">
            By clicking “Create Account”, you agree to AgroTech SL’s 
            <a href="/terms-and-conditions" class="text-success">Terms</a>, 
            <a href="/services-fee-agreement" class="text-success">Service Fee Agreement</a> 
            & <a href="/privacy-policy" class="text-success">Privacy Policy</a>.
          </small>
        </form>
      </div>
    </div>
  </div>

  <script>
    function togglePassword() {
      const password = document.getElementById('password');
      const toggleIcon = document.querySelector('.toggle-password');
      if (password.type === 'password') {
        password.type = 'text';
        toggleIcon.classList.replace('fa-eye-slash', 'fa-eye');
      } else {
        password.type = 'password';
        toggleIcon.classList.replace('fa-eye', 'fa-eye-slash');
      }
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
